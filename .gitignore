# Rendered manifests are build output, never commit them
rendered.yaml
rendered/

# Credentials - these must only ever exist in repo secrets or in the cluster
kubeconfig
kubeconfig.b64
authorized_keys
*.pem
*.key
secret-sftp.yaml

# Machine-local tooling settings
.claude/settings.local.json

# A weboldal tartalma SFTP-n keresztül kerül fel, nem a repóból települ
html/
design/

.DS_Store
Thumbs.db
